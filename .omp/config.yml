# Project-local omp settings for neo-matt maintainers.
# Global config may list `agents` under disabledProviders, which hides
# `.agents/skills`. customDirectories points at that tree so sync-upstream
# and ship stay discoverable in this repo only. Prefer this over touching
# disabledProviders in the project file.
skills:
  customDirectories:
    - .agents/skills
